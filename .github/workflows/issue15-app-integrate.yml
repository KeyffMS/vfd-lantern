name: Integrate issue 15 application state

on:
  push:
    branches: [agent/issue-15]

permissions:
  contents: write

jobs:
  integrate:
    if: github.actor != 'github-actions[bot]'
    runs-on: [self-hosted, linux, x64, podman, vfd-lantern]
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          ref: agent/issue-15
          fetch-depth: 0
      - name: Integrate application and polling runtime
        shell: bash
        run: |
          set -euo pipefail
          cat >/tmp/issue15_edit.rs <<'RS'
          use std::{fs, path::Path};

          fn read(path: &str) -> String {
              fs::read_to_string(path).unwrap_or_else(|error| panic!("read {path}: {error}"))
          }

          fn write(path: &str, content: String) {
              fs::write(path, content).unwrap_or_else(|error| panic!("write {path}: {error}"));
          }

          fn replace_once(path: &str, old: &str, new: &str) {
              let content = read(path);
              let count = content.matches(old).count();
              assert_eq!(count, 1, "{path}: expected one anchor, found {count}: {old:?}");
              write(path, content.replacen(old, new, 1));
          }

          fn replace_all(path: &str, old: &str, new: &str) {
              let content = read(path);
              let count = content.matches(old).count();
              assert!(count > 0, "{path}: missing anchor: {old:?}");
              write(path, content.replace(old, new));
          }

          fn insert_before(path: &str, marker: &str, inserted: &str) {
              replace_once(path, marker, &format!("{inserted}{marker}"));
          }

          fn main() {
              let app = "crates/lantern-app/src/application.rs";
              replace_once(
                  app,
                  r###"use crate::{
    AuditHealth, Authorization, BusError, ConnectionAction, ConnectionAttemptKind,
    ConnectionEffect, ConnectionFailure, ConnectionStep, ConnectionWizardState,
    ConnectionWizardView, Connectivity, MonitoringAction, MonitoringEffect,
    MonitoringRuntimeSnapshot, MonitoringView, OperationState, ProfileRegistry, ScopeSelection,
    SerialConnectError, SessionEffect, SessionFault, SessionInput, SessionState,
    SessionStateMachine, default_dashboard_parameters, identification_error_attempt,
    identification_report_export, project_monitoring_view,
};
"###,
                  r###"use crate::{
    AuditHealth, Authorization, BusError, ConnectionAction, ConnectionAttemptKind,
    ConnectionEffect, ConnectionFailure, ConnectionStep, ConnectionWizardState,
    ConnectionWizardView, Connectivity, MAX_PARAMETER_BROWSER_VISIBLE, MonitoringAction,
    MonitoringEffect, MonitoringRuntimeSnapshot, MonitoringView, OperationState, ParameterAction,
    ParameterBrowserView, ParameterDescriptorView, ParameterIntentContext, ProfileRegistry,
    ScopeSelection, SerialConnectError, SessionEffect, SessionFault, SessionInput, SessionState,
    SessionStateMachine, StagedWriteIntent, default_dashboard_parameters,
    identification_error_attempt, identification_report_export, parameter_catalog,
    prepare_parameter_intent, project_monitoring_view, project_parameter_browser_view,
};
"###,
              );

              insert_before(
                  app,
                  "#[derive(Clone, Debug)]\npub struct ApplicationState",
                  r###"#[derive(Clone, Debug)]
struct ApplicationParameterState {
    catalog: Arc<[ParameterDescriptorView]>,
    visible: Vec<ParameterId>,
    staged_intent: Option<StagedWriteIntent>,
    error: Option<String>,
}

impl Default for ApplicationParameterState {
    fn default() -> Self {
        Self {
            catalog: Vec::<ParameterDescriptorView>::new().into(),
            visible: Vec::new(),
            staged_intent: None,
            error: None,
        }
    }
}

impl ApplicationParameterState {
    fn for_profile(profile: &ValidatedDeviceProfile) -> Self {
        Self {
            catalog: parameter_catalog(profile),
            ..Self::default()
        }
    }
}

"###,
              );

              replace_once(
                  app,
                  "    monitoring: ApplicationMonitoringState,\n}",
                  "    monitoring: ApplicationMonitoringState,\n    parameters: ApplicationParameterState,\n}",
              );
              replace_all(
                  app,
                  "            monitoring: ApplicationMonitoringState::default(),\n",
                  "            monitoring: ApplicationMonitoringState::default(),\n            parameters: ApplicationParameterState::default(),\n",
              );

              replace_once(
                  app,
                  "        ApplicationView {\n            active_profile: self.active_profile.clone(),",
                  r###"        let parameters = if self.session.session_id().is_some() {
            self.active_profile
                .as_ref()
                .and_then(|id| self.registry.get(id))
                .map(|entry| {
                    project_parameter_browser_view(
                        entry.profile(),
                        entry.origin(),
                        Arc::clone(&self.parameters.catalog),
                        self.monitoring
                            .snapshot
                            .as_ref()
                            .map(|snapshot| Arc::clone(&snapshot.latest)),
                        self.parameters.staged_intent.clone(),
                        self.parameters.error.as_deref(),
                    )
                })
                .unwrap_or_default()
        } else {
            ParameterBrowserView::default()
        };
        ApplicationView {
            active_profile: self.active_profile.clone(),"###,
              );
              replace_once(
                  app,
                  "            monitoring,\n        }\n    }",
                  "            monitoring,\n            parameters,\n        }\n    }",
              );

              replace_all(
                  app,
                  "self.monitoring = ApplicationMonitoringState::default();",
                  "self.monitoring = ApplicationMonitoringState::default();\n                    self.parameters = ApplicationParameterState::default();",
              );
              // Two replacements above occur at different indentation levels. rustfmt normalizes the
              // expression indentation; the inserted statement remains in the same enclosing block.

              replace_once(
                  app,
                  "            ApplicationAction::Monitoring(action) => self.reduce_monitoring(action),\n",
                  "            ApplicationAction::Monitoring(action) => self.reduce_monitoring(action),\n            ApplicationAction::Parameters(action) => self.reduce_parameters(action),\n",
              );

              insert_before(
                  app,
                  "    fn reduce_monitoring(&mut self, action: MonitoringAction) -> Vec<ApplicationEffect> {",
                  r###"    fn reduce_parameters(&mut self, action: ParameterAction) -> Vec<ApplicationEffect> {
        let Some(profile) = self.selected_profile() else {
            self.parameters.error = Some("parameter browser has no active validated profile".to_owned());
            return Vec::new();
        };
        match action {
            ParameterAction::SetVisible(parameter_ids) => {
                if self.session.session_id().is_none() {
                    self.parameters.error = Some(
                        "parameter browser requires a Verified logical session".to_owned(),
                    );
                    return Vec::new();
                }
                let mut visible = Vec::new();
                for parameter_id in parameter_ids
                    .into_iter()
                    .take(MAX_PARAMETER_BROWSER_VISIBLE)
                {
                    if profile.parameter(&parameter_id).is_none() {
                        self.parameters.error = Some(format!(
                            "parameter {parameter_id} is not present in the active validated profile"
                        ));
                        return Vec::new();
                    }
                    if !visible.contains(&parameter_id) {
                        visible.push(parameter_id);
                    }
                }
                self.parameters.visible = visible.clone();
                self.parameters.error = None;
                vec![ApplicationEffect::Monitoring(
                    MonitoringEffect::SetParameterBrowser { parameters: visible },
                )]
            }
            ParameterAction::Refresh(parameter_id) => {
                if !matches!(
                    self.session.state(),
                    SessionState::Active(active)
                        if matches!(&active.connectivity, Connectivity::Connected)
                ) {
                    self.parameters.error = Some(
                        "parameter refresh requires a Verified connected session".to_owned(),
                    );
                    return Vec::new();
                }
                if profile.parameter(&parameter_id).is_none() {
                    self.parameters.error = Some(format!(
                        "parameter {parameter_id} is not present in the active validated profile"
                    ));
                    return Vec::new();
                }
                self.parameters.error = None;
                vec![ApplicationEffect::Monitoring(
                    MonitoringEffect::RefreshParameter { parameter_id },
                )]
            }
            ParameterAction::PrepareIntent {
                parameter_id,
                input,
            } => {
                let context = match self.session.state() {
                    SessionState::Active(active)
                        if matches!(&active.connectivity, Connectivity::Connected) =>
                    {
                        ParameterIntentContext {
                            session_id: active.session_id,
                            fingerprint: active.identity.device.fingerprint.clone(),
                            profile_hash: active.identity.profile_hash.to_hex(),
                            process_writes_enabled: !matches!(
                                &active.authorization,
                                Authorization::ProcessDisabled
                            ),
                        }
                    }
                    _ => {
                        self.parameters.error = Some(
                            "parameter editor requires a Verified connected session".to_owned(),
                        );
                        return Vec::new();
                    }
                };
                let Some(snapshot) = self.monitoring.snapshot.as_ref() else {
                    self.parameters.error = Some(
                        "parameter editor has no telemetry snapshot yet".to_owned(),
                    );
                    return Vec::new();
                };
                match prepare_parameter_intent(
                    &profile,
                    snapshot.latest.as_ref(),
                    context,
                    &parameter_id,
                    &input,
                ) {
                    Ok(staged) => {
                        self.parameters.staged_intent = Some(staged);
                        self.parameters.error = None;
                    }
                    Err(error) => {
                        self.parameters.staged_intent = None;
                        self.parameters.error = Some(error.to_string());
                    }
                }
                Vec::new()
            }
            ParameterAction::ClearIntent => {
                self.parameters.staged_intent = None;
                self.parameters.error = None;
                Vec::new()
            }
        }
    }

"###,
              );

              replace_once(
                  app,
                  "                        self.monitoring = ApplicationMonitoringState::for_profile(&profile);\n",
                  "                        self.monitoring = ApplicationMonitoringState::for_profile(&profile);\n                        self.parameters = ApplicationParameterState::for_profile(&profile);\n",
              );

              replace_once(
                  app,
                  "    monitoring: MonitoringView,\n}",
                  "    monitoring: MonitoringView,\n    parameters: ParameterBrowserView,\n}",
              );
              replace_once(
                  app,
                  "            monitoring: MonitoringView::default(),\n",
                  "            monitoring: MonitoringView::default(),\n            parameters: ParameterBrowserView::default(),\n",
              );
              replace_once(
                  app,
                  r###"    pub const fn monitoring(&self) -> &MonitoringView {
        &self.monitoring
    }
}
"###,
                  r###"    pub const fn monitoring(&self) -> &MonitoringView {
        &self.monitoring
    }

    #[must_use]
    pub const fn parameters(&self) -> &ParameterBrowserView {
        &self.parameters
    }
}
"###,
              );
              replace_once(
                  app,
                  "    Monitoring(MonitoringAction),\n    Session(SessionInput),",
                  "    Monitoring(MonitoringAction),\n    Parameters(ParameterAction),\n    Session(SessionInput),",
              );

              let monitoring = "crates/lantern-app/src/monitoring.rs";
              replace_once(
                  monitoring,
                  r###"    Reconfigure {
        dashboard_parameters: Vec<ParameterId>,
        scope: ScopeSelection,
    },
    ClearHistory {
"###,
                  r###"    Reconfigure {
        dashboard_parameters: Vec<ParameterId>,
        scope: ScopeSelection,
    },
    SetParameterBrowser {
        parameters: Vec<ParameterId>,
    },
    RefreshParameter {
        parameter_id: ParameterId,
    },
    ClearHistory {
"###,
              );

              let parameters = "crates/lantern-app/src/parameters.rs";
              replace_once(
                  parameters,
                  "use lantern_profile::{ReadBackPolicy, ValidatedDeviceProfile, ValidatedParameter};\n",
                  "use lantern_profile::{ReadBackPolicy, ValidatedDeviceProfile, ValidatedParameter};\nuse sha2::{Digest, Sha256};\n",
              );
              replace_once(
                  parameters,
                  "            SubscriberId::parse(format!(\"parameters:{}\", parameter_id.as_str()))?,\n",
                  "            parameter_subscriber_id(\"parameters\", parameter_id)?,\n",
              );
              insert_before(
                  parameters,
                  "pub fn prepare_parameter_intent(\n",
                  r###"pub fn parameter_refresh_subscription(
    profile: &ValidatedDeviceProfile,
    parameter_id: &ParameterId,
) -> Result<ReadSubscription, ParameterBrowserError> {
    if profile.parameter(parameter_id).is_none() {
        return Err(ParameterBrowserError::UnknownParameter(parameter_id.clone()));
    }
    Ok(ReadSubscription::new(
        parameter_id.clone(),
        FrequencyClass::Fast,
        parameter_subscriber_id("parameters-refresh", parameter_id)?,
        SubscriptionReason::ParameterBrowser,
        false,
        Duration::from_millis(500),
    )?)
}

fn parameter_subscriber_id(
    prefix: &str,
    parameter_id: &ParameterId,
) -> Result<SubscriberId, ParameterBrowserError> {
    let digest = Sha256::digest(parameter_id.as_str().as_bytes());
    let token = digest[..12]
        .iter()
        .map(|byte| format!("{byte:02x}"))
        .collect::<String>();
    Ok(SubscriberId::parse(format!("{prefix}:{token}"))?)
}

"###,
              );

              let runtime = "crates/vfd-lantern/src/monitoring_runtime.rs";
              replace_once(
                  runtime,
                  "    ValidatedDeviceProfile, ValidatedSettings, dashboard_subscriptions, scope_subscriptions,\n",
                  "    ValidatedDeviceProfile, ValidatedSettings, dashboard_subscriptions,\n    parameter_browser_subscriptions, parameter_refresh_subscription, scope_subscriptions,\n",
              );
              replace_once(
                  runtime,
                  "const MAX_RENDER_HISTORY_POINTS: usize = 512;\n",
                  "const MAX_RENDER_HISTORY_POINTS: usize = 512;\nconst PARAMETER_BROWSER_DEBOUNCE: Duration = Duration::from_millis(120);\n",
              );
              replace_once(
                  runtime,
                  r###"struct MonitoringState {
    bus: Option<BusActorHandle>,
    verified_session: Option<SessionId>,
    active: Option<ActiveMonitoring>,
}
"###,
                  r###"struct MonitoringState {
    bus: Option<BusActorHandle>,
    verified_session: Option<SessionId>,
    active: Option<ActiveMonitoring>,
    parameter_browser_generation: u64,
    refresh_generation: u64,
}
"###,
              );
              replace_once(
                  runtime,
                  "    scope: ScopeSelection,\n    poll: PollExecutorHandle,",
                  "    scope: ScopeSelection,\n    parameter_browser_parameters: Vec<ParameterId>,\n    pending_parameter_browser_parameters: Vec<ParameterId>,\n    poll: PollExecutorHandle,",
              );

              replace_once(
                  runtime,
                  r###"            MonitoringEffect::Reconfigure { .. }
            | MonitoringEffect::ClearHistory { .. }
            | MonitoringEffect::Stop => self.current_session(),
"###,
                  r###"            MonitoringEffect::Reconfigure { .. }
            | MonitoringEffect::SetParameterBrowser { .. }
            | MonitoringEffect::RefreshParameter { .. }
            | MonitoringEffect::ClearHistory { .. }
            | MonitoringEffect::Stop => self.current_session(),
"###,
              );
              replace_once(
                  runtime,
                  r###"            MonitoringEffect::Reconfigure {
                dashboard_parameters,
                scope,
            } => self.reconfigure(dashboard_parameters, scope),
            MonitoringEffect::ClearHistory { parameter_ids } => self.clear_history(&parameter_ids),
"###,
                  r###"            MonitoringEffect::Reconfigure {
                dashboard_parameters,
                scope,
            } => self.reconfigure(dashboard_parameters, scope),
            MonitoringEffect::SetParameterBrowser { parameters } => {
                self.set_parameter_browser(parameters)
            }
            MonitoringEffect::RefreshParameter { parameter_id } => {
                self.refresh_parameter(parameter_id)
            }
            MonitoringEffect::ClearHistory { parameter_ids } => self.clear_history(&parameter_ids),
"###,
              );
              replace_once(
                  runtime,
                  "        let subscriptions = monitoring_subscriptions(&profile, &dashboard_parameters, &scope)?;\n",
                  "        let subscriptions =\n            monitoring_subscriptions(&profile, &dashboard_parameters, &scope, &[])?;\n",
              );
              replace_once(
                  runtime,
                  "                dashboard_parameters,\n                scope,\n                poll,",
                  "                dashboard_parameters,\n                scope,\n                parameter_browser_parameters: Vec::new(),\n                pending_parameter_browser_parameters: Vec::new(),\n                poll,",
              );

              let old_reconfigure = r###"    fn reconfigure(
        &self,
        dashboard_parameters: Vec<ParameterId>,
        scope: ScopeSelection,
    ) -> Result<(), String> {
        let mut state = lock_state(&self.shared.state);
        let active = state
            .active
            .as_mut()
            .ok_or_else(|| "monitoring runtime is not active".to_owned())?;
        let subscriptions =
            monitoring_subscriptions(&active.profile, &dashboard_parameters, &scope)?;
        let plan = Arc::new(
            active
                .planner
                .build(
                    &active.profile,
                    subscriptions,
                    active.planner_config,
                    Instant::now(),
                )
                .map_err(|error| error.to_string())?,
        );
        validate_monitoring_plan(&plan)?;
        active
            .pipeline
            .update_plan(Arc::clone(&plan))
            .map_err(|error| error.to_string())?;
        active
            .poll
            .update_plan(Arc::clone(&plan))
            .map_err(|error| error.to_string())?;
        active.plan = plan;
        active.dashboard_parameters = dashboard_parameters;
        active.scope = scope;
        Ok(())
    }

"###;
              let new_reconfigure = r###"    fn reconfigure(
        &self,
        dashboard_parameters: Vec<ParameterId>,
        scope: ScopeSelection,
    ) -> Result<(), String> {
        let parameters = {
            let state = lock_state(&self.shared.state);
            state
                .active
                .as_ref()
                .map(|active| active.parameter_browser_parameters.clone())
                .ok_or_else(|| "monitoring runtime is not active".to_owned())?
        };
        self.reconfigure_all(dashboard_parameters, scope, parameters)
    }

    fn reconfigure_all(
        &self,
        dashboard_parameters: Vec<ParameterId>,
        scope: ScopeSelection,
        parameter_browser_parameters: Vec<ParameterId>,
    ) -> Result<(), String> {
        let mut state = lock_state(&self.shared.state);
        let active = state
            .active
            .as_mut()
            .ok_or_else(|| "monitoring runtime is not active".to_owned())?;
        let subscriptions = monitoring_subscriptions(
            &active.profile,
            &dashboard_parameters,
            &scope,
            &parameter_browser_parameters,
        )?;
        let plan = Arc::new(
            active
                .planner
                .build(
                    &active.profile,
                    subscriptions,
                    active.planner_config,
                    Instant::now(),
                )
                .map_err(|error| error.to_string())?,
        );
        validate_monitoring_plan(&plan)?;
        active
            .pipeline
            .update_plan(Arc::clone(&plan))
            .map_err(|error| error.to_string())?;
        active
            .poll
            .update_plan(Arc::clone(&plan))
            .map_err(|error| error.to_string())?;
        active.plan = plan;
        active.dashboard_parameters = dashboard_parameters;
        active.scope = scope;
        active.parameter_browser_parameters = parameter_browser_parameters;
        Ok(())
    }

    fn set_parameter_browser(&self, parameters: Vec<ParameterId>) -> Result<(), String> {
        let (session_id, generation) = {
            let mut state = lock_state(&self.shared.state);
            state.parameter_browser_generation = state.parameter_browser_generation.saturating_add(1);
            let generation = state.parameter_browser_generation;
            let active = state
                .active
                .as_mut()
                .ok_or_else(|| "monitoring runtime is not active".to_owned())?;
            for parameter_id in &parameters {
                if active.profile.parameter(parameter_id).is_none() {
                    return Err(format!(
                        "parameter {parameter_id} is not present in the active validated profile"
                    ));
                }
            }
            active.pending_parameter_browser_parameters = parameters;
            (active.session_id, generation)
        };
        let runtime = self.clone();
        let action_tx = self.action_tx.clone();
        tokio::spawn(async move {
            tokio::time::sleep(PARAMETER_BROWSER_DEBOUNCE).await;
            if let Err(message) = runtime.apply_pending_parameter_browser(session_id, generation) {
                let _ = action_tx.send(ApplicationAction::Monitoring(
                    MonitoringAction::RuntimeFailed {
                        session_id,
                        message,
                    },
                ));
            }
        });
        Ok(())
    }

    fn apply_pending_parameter_browser(
        &self,
        session_id: SessionId,
        generation: u64,
    ) -> Result<(), String> {
        let (dashboard, scope, parameters) = {
            let state = lock_state(&self.shared.state);
            if state.parameter_browser_generation != generation {
                return Ok(());
            }
            let active = state
                .active
                .as_ref()
                .filter(|active| active.session_id == session_id)
                .ok_or_else(|| "parameter browser session is no longer active".to_owned())?;
            (
                active.dashboard_parameters.clone(),
                active.scope.clone(),
                active.pending_parameter_browser_parameters.clone(),
            )
        };
        self.reconfigure_all(dashboard, scope, parameters)
    }

    fn refresh_parameter(&self, parameter_id: ParameterId) -> Result<(), String> {
        let (session_id, generation, delay) = {
            let mut state = lock_state(&self.shared.state);
            state.refresh_generation = state.refresh_generation.saturating_add(1);
            let generation = state.refresh_generation;
            let active = state
                .active
                .as_mut()
                .ok_or_else(|| "monitoring runtime is not active".to_owned())?;
            if active.profile.parameter(&parameter_id).is_none() {
                return Err(format!(
                    "parameter {parameter_id} is not present in the active validated profile"
                ));
            }
            let mut subscriptions = monitoring_subscriptions(
                &active.profile,
                &active.dashboard_parameters,
                &active.scope,
                &active.parameter_browser_parameters,
            )?;
            subscriptions.push(
                parameter_refresh_subscription(&active.profile, &parameter_id)
                    .map_err(|error| error.to_string())?,
            );
            let plan = Arc::new(
                active
                    .planner
                    .build(
                        &active.profile,
                        subscriptions,
                        active.planner_config,
                        Instant::now(),
                    )
                    .map_err(|error| error.to_string())?,
            );
            validate_monitoring_plan(&plan)?;
            active
                .pipeline
                .update_plan(Arc::clone(&plan))
                .map_err(|error| error.to_string())?;
            active
                .poll
                .update_plan(Arc::clone(&plan))
                .map_err(|error| error.to_string())?;
            active.plan = plan;
            let delay = Duration::from_millis(
                self.settings
                    .polling
                    .telemetry_critical_ms
                    .saturating_mul(2)
                    .max(200),
            );
            (active.session_id, generation, delay)
        };
        let runtime = self.clone();
        let action_tx = self.action_tx.clone();
        tokio::spawn(async move {
            tokio::time::sleep(delay).await;
            if let Err(message) = runtime.restore_after_refresh(session_id, generation) {
                let _ = action_tx.send(ApplicationAction::Monitoring(
                    MonitoringAction::RuntimeFailed {
                        session_id,
                        message,
                    },
                ));
            }
        });
        Ok(())
    }

    fn restore_after_refresh(
        &self,
        session_id: SessionId,
        generation: u64,
    ) -> Result<(), String> {
        let (dashboard, scope, parameters) = {
            let state = lock_state(&self.shared.state);
            if state.refresh_generation != generation {
                return Ok(());
            }
            let active = state
                .active
                .as_ref()
                .filter(|active| active.session_id == session_id)
                .ok_or_else(|| "parameter refresh session is no longer active".to_owned())?;
            (
                active.dashboard_parameters.clone(),
                active.scope.clone(),
                active.parameter_browser_parameters.clone(),
            )
        };
        self.reconfigure_all(dashboard, scope, parameters)
    }

"###;
              replace_once(runtime, old_reconfigure, new_reconfigure);

              replace_once(
                  runtime,
                  "            state.verified_session = None;\n            state.active.take()\n",
                  "            state.verified_session = None;\n            state.parameter_browser_generation =\n                state.parameter_browser_generation.saturating_add(1);\n            state.refresh_generation = state.refresh_generation.saturating_add(1);\n            state.active.take()\n",
              );

              replace_once(
                  runtime,
                  r###"fn monitoring_subscriptions(
    profile: &ValidatedDeviceProfile,
    dashboard_parameters: &[ParameterId],
    scope: &ScopeSelection,
) -> Result<Vec<ReadSubscription>, String> {
    let mut subscriptions = dashboard_subscriptions(profile, dashboard_parameters)
        .map_err(|error| error.to_string())?;
    subscriptions.extend(scope_subscriptions(profile, scope).map_err(|error| error.to_string())?);
    Ok(subscriptions)
}
"###,
                  r###"fn monitoring_subscriptions(
    profile: &ValidatedDeviceProfile,
    dashboard_parameters: &[ParameterId],
    scope: &ScopeSelection,
    parameter_browser_parameters: &[ParameterId],
) -> Result<Vec<ReadSubscription>, String> {
    let mut subscriptions = dashboard_subscriptions(profile, dashboard_parameters)
        .map_err(|error| error.to_string())?;
    subscriptions.extend(scope_subscriptions(profile, scope).map_err(|error| error.to_string())?);
    subscriptions.extend(
        parameter_browser_subscriptions(profile, parameter_browser_parameters)
            .map_err(|error| error.to_string())?,
    );
    Ok(subscriptions)
}
"###,
              );

              assert!(Path::new("crates/lantern-tui/src/parameter_state.rs").exists());
          }
          RS
          rustc --edition=2024 /tmp/issue15_edit.rs -o /tmp/issue15_edit
          /tmp/issue15_edit
          cargo fmt --all
          cargo check --workspace --all-features --locked
          cargo test --locked -p lantern-app --all-features
          git rm .github/workflows/issue15-app-integrate.yml
          git add -A
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git commit -m 'feat(#15): integrate parameter browser with application polling'
          git push origin HEAD:agent/issue-15
