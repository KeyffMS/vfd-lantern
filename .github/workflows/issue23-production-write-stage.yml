name: Stage issue 23 production write

on:
  push:
    branches:
      - agent/issue-23
    paths:
      - '.github/scripts/issue23_production_write_stage.rs'
      - '.github/workflows/issue23-production-write-stage.yml'
      - 'crates/lantern-app/**'
      - 'crates/vfd-lantern/**'
      - 'scripts/check-architecture.sh'

permissions:
  contents: write

jobs:
  production-write:
    runs-on: [self-hosted, linux, x64, podman, vfd-lantern]
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          ref: agent/issue-23
          fetch-depth: 0
      - name: Integrate and validate production guarded write composition
        shell: bash
        run: |
          set -euo pipefail
          rustc --edition=2024 .github/scripts/issue23_production_write_stage.rs -o /tmp/issue23-production-write-stage
          /tmp/issue23-production-write-stage
          cargo fmt --all
          cargo check --workspace --all-features --locked
          cargo test --workspace --all-features --locked
          cargo clippy --workspace --all-targets --all-features --locked -- -D warnings
          RUSTDOCFLAGS='-D warnings' cargo doc --workspace --all-features --no-deps --locked
          sh scripts/check-architecture.sh
          git rm .github/workflows/issue23-production-write-stage.yml
          git rm .github/scripts/issue23_production_write_stage.rs
          git add -A
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git commit -m 'feat(#23): wire guarded writes into production composition root'
          git push origin HEAD:agent/issue-23
